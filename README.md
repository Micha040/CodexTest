# CodexTest

This repository contains **CoolApp**, a small command-line application that prints
random motivational messages in style.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```

## Usage

Run the application with:

```bash
python -m coolapp.main
```

## Testing

Run the tests with:

```bash
pytest
```
